--- ImageMagick-7.1.2-29/MagickCore/mutex.h
+++ ImageMagick-7.1.2-29/MagickCore/mutex.h
@@ -30,7 +30,11 @@ extern "C" {
   independent means of synchronization to each such translation unit.
 */
 
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+/*
+  Prefer POSIX mutexes over omp_lock_t when both are available.
+  omp_lock_t is not fork-safe and breaks prefork servers (php-fpm).
+*/
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
 static MagickBooleanType
   translation_unit_initialized = MagickFalse;
 
@@ -46,7 +50,7 @@ static LONG
 
 static inline void DestroyMagickMutex(void)
 {
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   if (translation_unit_initialized != MagickFalse)
     omp_destroy_lock(&translation_unit_mutex);
   translation_unit_initialized=MagickFalse;
@@ -55,7 +59,7 @@ static inline void DestroyMagickMutex(vo
 
 static inline void InitializeMagickMutex(void)
 {
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   if (translation_unit_initialized == MagickFalse)
     omp_init_lock(&translation_unit_mutex);
   translation_unit_initialized=MagickTrue;
@@ -64,7 +68,7 @@ static inline void InitializeMagickMutex
 
 static inline void LockMagickMutex(void)
 {
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   if (translation_unit_initialized == MagickFalse)
     InitializeMagickMutex();
   omp_set_lock(&translation_unit_mutex);
@@ -88,7 +92,7 @@ static inline void LockMagickMutex(void)
 
 static inline void UnlockMagickMutex(void)
 {
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   if (translation_unit_initialized == MagickFalse)
     InitializeMagickMutex();
   omp_unset_lock(&translation_unit_mutex);
--- ImageMagick-7.1.2-29/MagickCore/semaphore.c
+++ ImageMagick-7.1.2-29/MagickCore/semaphore.c
@@ -205,7 +205,7 @@ MagickExport SemaphoreInfo *AcquireSemap
   /*
     Initialize the semaphore.
   */
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   omp_init_lock((omp_lock_t *) &semaphore_info->mutex);
 #elif defined(MAGICKCORE_THREAD_SUPPORT)
   {
@@ -302,7 +302,7 @@ MagickExport void LockSemaphoreInfo(Sema
       (void) fflush(stderr);
     }
 #endif
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   omp_set_lock((omp_lock_t *) &semaphore_info->mutex);
 #elif defined(MAGICKCORE_THREAD_SUPPORT)
   {
@@ -354,7 +354,7 @@ MagickExport void RelinquishSemaphoreInf
   assert((*semaphore_info) != (SemaphoreInfo *) NULL);
   assert((*semaphore_info)->signature == MagickCoreSignature);
   LockMagickMutex();
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   omp_destroy_lock((omp_lock_t *) &(*semaphore_info)->mutex);
 #elif defined(MAGICKCORE_THREAD_SUPPORT)
   {
@@ -461,7 +461,7 @@ MagickExport void UnlockSemaphoreInfo(Se
     }
   semaphore_info->reference_count--;
 #endif
-#if defined(MAGICKCORE_OPENMP_SUPPORT)
+#if defined(MAGICKCORE_OPENMP_SUPPORT) && !defined(MAGICKCORE_THREAD_SUPPORT)
   omp_unset_lock((omp_lock_t *) &semaphore_info->mutex);
 #elif defined(MAGICKCORE_THREAD_SUPPORT)
   {
